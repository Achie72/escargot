function assert (a){ if (a != true){ throw Error("Assert failed"); }}
class Shape {
    constructor (id, x, y) {
        this.id = id
        this.move(x, y)
    }
    move (x, y) {
        this.x = x
        this.y = y
    }
}